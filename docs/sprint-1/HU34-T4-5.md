<!-- 
1. AuthService — guardar el rol del usuario
Al hacer login guardar role en localStorage, y agregar método getRole().

2. RoleGuard — nuevo guard
Verifica que el usuario tenga el rol requerido para entrar a una ruta.

3. Ruta /app/usuarios + componente Usuarios
Tabla con usuarios, selector de rol, llama al PATCH. Solo accesible por admin.

4. AuthService — método assignRole()
Llama a PATCH /api/auth/users/<id>/role/.
 -->